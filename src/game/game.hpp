#ifndef GAME_H
#define GAME_H

class Game {
    private:
        // listPLayer (Player) list player 
        // poin (long int) poin saat ini
        // deckCard (list of card) list kartu yang belum dibagi
        // ronde keberapa (int)
        // urutan (list of int)
        // giliran (int)
        // isFinish (boolean) untuk menandakan apakah sudah ada pemenangnya
        // canUseAbility (boolean) untuk menandakan apakah ability dimatikan atau tidak 
    public:
};

#endif