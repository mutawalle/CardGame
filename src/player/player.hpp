#ifndef PLAYER_H
#define PLAYER_H

#include <string>


class Player {
    private:
        // nama (string)
        // playerCard (PlayerCard)
        // ability (string) "" berarti tidak punya ability
        // poin
    public:
        double value();
};

#endif