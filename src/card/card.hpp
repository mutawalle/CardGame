// parent dari card
#ifndef CARD_HPP
#define CARD_HPP

#include "abstractValue.hpp"

class Card : AbstractValue {
    private:
        // number (int)
        // color (string) 
    public:
        // override value()
};

#endif